# CrackDetect - Python Dependencies
# torch + torchvision are installed separately via start.bat (CUDA version)

# UI
customtkinter>=5.2.0

# Geo
rasterio>=1.3.0
shapely>=2.0.0

# CAD Export
ezdxf>=1.1.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0
scikit-image>=0.21.0

# U-Net ONNX inference (trained model)
onnxruntime-gpu>=1.17.0   # GPU version; for CPU-only: onnxruntime
