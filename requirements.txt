# CrackDetect – Python Abhängigkeiten
# torch + torchvision werden separat via start.bat installiert (CUDA-Version)

# UI
gradio>=4.44.0

# Geo
rasterio>=1.3.0
shapely>=2.0.0

# CAD Export
ezdxf>=1.1.0

# Bild-Verarbeitung
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.8.0

# SAM3 Hilfsbibliotheken
huggingface_hub>=0.24.0
tqdm>=4.66.0

# SAM3 required dependencies (auch via start.bat installiert)
einops
pycocotools
triton-windows

# PyTorch with CUDA – installed by start.bat:
#   pip install torch==2.10.0 torchvision --index-url https://download.pytorch.org/whl/cu128
#
# SAM3 – installed by start.bat:
#   pip install git+https://github.com/facebookresearch/sam3.git
#
# SAM3 checkpoint – HuggingFace access required:
#   huggingface-cli login
#   huggingface-cli download facebook/sam3
